function madLib(verb, adjective, noun) {
  return `We shall ${verb} the ${adjective} ${noun}`
}

// madLib('squish', 'squishy', 'squanch')

function isSubstring(searchString, subString) {
  return searchString === subString
}

isSubstring("a", "a")
